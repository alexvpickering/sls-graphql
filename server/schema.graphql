type Query {
  getThing(id: ID!): Thing
  hello: String
}

type Mutation {
  createThing(one: String, two: String): Thing
}

type Thing {
  id: ID!
  one: String
  two: String
}

schema {
  query: Query
  mutation: Mutation
}
